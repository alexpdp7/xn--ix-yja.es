# git

## Create a repository

You own `/srv/repos/$USER`.
You can put repositories there to expose them at `https://host/cgit/`.
cgit supports HTTPS public cloning from `https://host/cgit/$USER/$REPO.git`.
